# Copyright (c) 2026 The ZMK Contributors
# SPDX-License-Identifier: MIT

description: |
  Keymap layer indicator: paints the targeted LEDs while a given layer is active.
  Declare one node per layer that should have its own LED.

compatible: "keypaw,rgb-indicator-layer"

include: ["keypaw,rgb-indicator-common.yaml"]

properties:
  layer:
    type: int
    required: true
    description: Layer id this indicator watches.

  only-topmost:
    type: boolean
    description: |
      Paint only while this is the highest active layer. By default the indicator
      follows "this layer is active", which is what a momentary layer key wants.
